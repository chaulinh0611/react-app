
export const Authenticate = {
  AUTH: "auth_token",
  REFRESH_TOKEN_ETC: "refresh_token",
};

export const keyStorage = {
  ACCESS_TOKEN: "access_token",
  REFRESH_TOKEN: "refresh_token",
};

export const keyHeader = "my-secret-api-key";
